akka.javasdk {
  dev-mode { http-port = 8086 }
  agent {
    model-provider = openai
    openai {
      model-name = "gpt-4o-mini"
      api-key = ${?OPENAI_API_KEY}
    }
  }
}

akka.loglevel = "INFO"
