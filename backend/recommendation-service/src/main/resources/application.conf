akka.javasdk {
  dev-mode { http-port = 8084 }
}
akka.loglevel = "INFO"

# Cross-service URLs (override with env vars for deployed environments)
services {
  analysis-url = "http://localhost:8083"
  analysis-url = ${?ANALYSIS_SERVICE_URL}
}
